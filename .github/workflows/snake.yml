name: Generate Snake

on:
  schedule:
    # Tự động chạy mỗi 6 giờ
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Generate Snake
        uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            # dist/github-snake.svg
            # dist/github-snake-dark.svg?palette=github-dark
            dist/github-snake.svg?palette=github-light&hide_weeks=false
            dist/github-snake-dark.svg?palette=github-dark&hide_weeks=false

      - name: Push to GitHub
        uses: crazy-max/ghaction-github-pages@v3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
